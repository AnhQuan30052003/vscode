// Chong gach //
#include <iostream>
#include <algorithm>
using namespace std;

int dem_tang(int* a, int n) {
    sort(a, a+n);
   

    return dem;
}

int main() {
    int n;
    cin >> n;
    int a[n];
    for (int i = 0; i < n; i++)
        cin >> a[i];
    cout << dem_tang(a, n);
}